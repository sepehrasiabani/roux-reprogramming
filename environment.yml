name: roux-reprogramming
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy==1.26.4
      - pandas>=2.0
      - scanpy>=1.9
      - anndata>=0.10
      - scikit-learn>=1.3
      - matplotlib>=3.6
      - scrublet
      - celloracle==0.22.0
      - python-igraph
      - leidenalg
      - louvain
      - genomepy
      - gimmemotifs
      - goatools
      - pybedtools
      - velocyto
      - fa2-modified
